export default function DoctorsPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-8">
      <h1 className="text-xl font-bold text-text-primary">Doctors</h1>
      <p className="mt-1 text-sm text-text-secondary">
        Doctor scraping lives here next — same fetch → trim → extract flow, per-facility doctor lists.
      </p>
      <div className="mt-4 rounded-2xl border border-border bg-surface p-6 text-center shadow-sm">
        <span className="inline-block rounded-full bg-surface-alt px-3 py-1 text-xs font-medium text-text-secondary">
          coming soon
        </span>
      </div>
    </main>
  );
}
